<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Profile</title>
  <link rel="stylesheet" href="/css/styles.css">

</head>
<body>
  <%- include('partials/navbar') %>

  <h1>Your Profile</h1>
  <form action="/profile" method="POST">
    <label>Age: <input type="number" name="age" value="<%= profile.age %>" required></label><br>
    <label>Gender: 
      <select name="gender" required>
        <option value="male" <%= profile.gender === 'male' ? 'selected' : '' %>>Male</option>
        <option value="female" <%= profile.gender === 'female' ? 'selected' : '' %>>Female</option>
      </select>
    </label><br>
    <label>Height (cm): <input type="number" name="height" step="0.01" value="<%= profile.height %>" required></label><br>
    <label>Weight (kg): <input type="number" name="weight" step="0.01" value="<%= profile.weight %>" required></label><br>
    <label>Goal Weight (kg): <input type="number" name="goal_weight" step="0.01" value="<%= profile.goal_weight %>" required></label><br>
    <label>Belly Circumference (cm): <input type="number" name="belly_circumference" step="0.01" value="<%= profile.belly_circumference %>" required></label><br>
    <label>Neck Circumference (cm): <input type="number" name="neck_circumference" step="0.01" value="<%= profile.neck_circumference %>" required></label><br>

    <button type="submit">Save Profile</button>
  </form>

  <form action="/profile/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete your profile?');">
    <button type="submit">Delete Profile</button>
  </form>
  
</body>
</html>
